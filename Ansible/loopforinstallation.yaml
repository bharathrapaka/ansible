- name: loopforinstallation
  hosts: localhost
  become: yes
  tasks:
  - name: package installation
    ansible.builtin.package:
      name: "{{items}}"
      state: present

    loop:
    - git
    - gcc
    - make